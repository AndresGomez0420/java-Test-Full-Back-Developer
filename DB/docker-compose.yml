version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: hackathon
    environment:
      POSTGRES_USER: admin_hackathon
      POSTGRES_PASSWORD: hackathon0420!
      POSTGRES_DB: hackathon
    ports:
      - "5435:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
 
volumes:
  postgres_data:
    driver: local
